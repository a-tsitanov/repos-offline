# Без кэша и без расшифровки TLS: пропускает всё, пишет хосты в access.log.
http_port 3128
http_access allow all
cache deny all
access_log stdio:/var/log/squid/access.log squid
pid_filename none
shutdown_lifetime 1 seconds
